<Window x:Class="MultiStreamViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiStreamViewer"
        xmlns:vm="clr-namespace:MultiStreamViewer.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">
    <Window.DataContext>
        <local:ResolveExtension Type="{x:Type vm:MainWindowViewModel}" />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Menu Grid.Row="0" Grid.ColumnSpan="2">
            <MenuItem Header="View">
                <MenuItem x:Name="Streams_MenuItem" IsCheckable="True" Header="Streams"/>
            </MenuItem>
            <MenuItem Header="Layout">
                <MenuItem Header="Option 1" IsCheckable="True" IsChecked="{Binding SelectedViewMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Option1}" Command="{Binding SetLayoutCommand}" CommandParameter="Option1" />
                <MenuItem Header="Option 2" IsCheckable="True" IsChecked="{Binding SelectedViewMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Option2}" Command="{Binding SetLayoutCommand}" CommandParameter="Option2" />
                <MenuItem Header="Option 3" IsCheckable="True" IsChecked="{Binding SelectedViewMode, Converter={StaticResource EnumToBoolConverter}, ConverterParameter=Option3}" Command="{Binding SetLayoutCommand}" CommandParameter="Option3" />
            </MenuItem>
        </Menu>
        <ListView x:Name="StreamList" Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Streams}" Visibility="{Binding ElementName=Streams_MenuItem, Path=IsChecked, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" MouseMove="TextBlock_MouseMove" Padding="5" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ItemsControl Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Cameras}">
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type vm:CameraViewModel}">
                    <local:CameraView />
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid IsItemsHost="True" Columns="{Binding Columns}" Rows="{Binding Rows}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</Window>
