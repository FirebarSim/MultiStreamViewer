<UserControl x:Class="MultiStreamViewer.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiStreamViewer"
             xmlns:ffme="clr-namespace:Unosquare.FFME;assembly=ffme.win"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" Margin="6" Padding="6" Background="Transparent" AllowDrop="True" Drop="Border_Drop" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding ModelName}" FontWeight="Bold" />
            <ffme:MediaElement Grid.Row="1" x:Name="Media" Background="Black" LoadedBehavior="Manual" UnloadedBehavior="Manual" Stretch="Uniform" />
            <TextBlock Grid.Row="1" Background="Red" Foreground="White" Text="BAE SYSTEMS" FontFamily="Franklin Gothic Heavy" FontStretch="ExtraExpanded" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5,2.5,5,2.5" Visibility="{Binding LogoVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="Black" Padding="3,1,3,1" Text="{Binding StatusMessage, FallbackValue='StatusMessage'}"  Visibility="{Binding StatusMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Open" Width="60" Margin="2" Command="{Binding OpenCommand}" />
                <Button Content="Play" Width="60" Margin="2" Command="{Binding PlayCommand}" />
                <Button Content="Pause" Width="60" Margin="2" Command="{Binding PauseCommand}" />
                <Button Content="Close" Width="60" Margin="2" Command="{Binding CloseCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
