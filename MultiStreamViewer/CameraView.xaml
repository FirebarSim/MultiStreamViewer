<UserControl x:Class="MultiStreamViewer.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiStreamViewer"
             xmlns:ffme="clr-namespace:Unosquare.FFME;assembly=ffme.win"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" Margin="6" Padding="3" Background="Transparent" AllowDrop="True" Drop="Border_Drop" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="3" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding ModelName}" FontWeight="Bold" />
            <ffme:MediaElement Grid.Row="1" x:Name="Media" Background="Black" LoadedBehavior="Manual" UnloadedBehavior="Manual" Stretch="Uniform" />
            <Grid Grid.Row="1" Background="Black" Visibility="{Binding LogoVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Image Grid.Column="1" Grid.Row="1" Stretch="Uniform" Source="/logo.png"/>
            </Grid>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Background="Black" Padding="3,1,3,1" Text="{Binding StatusMessage, FallbackValue='StatusMessage'}"  Visibility="{Binding StatusMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Play" Width="60" Margin="2" Command="{Binding PlayCommand}" IsEnabled="{Binding CanPlay}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}" >
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanPlay, FallbackValue=True}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Pause" Width="60" Margin="2" Command="{Binding PauseCommand}" IsEnabled="{Binding CanPause}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}" >
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CanPause, FallbackValue=True}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Mute" Width="60" Margin="2" Command="{Binding MuteCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMuted, FallbackValue=False}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Unmute" Width="60" Margin="2" Command="{Binding UnmuteCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMuted, FallbackValue=False}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Close" Width="60" Margin="2" Command="{Binding CloseCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
